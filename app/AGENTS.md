# AGENTS.md（永久约束）

1. 使用 Next.js App Router + React + Tailwind CSS；
2. 正文宽度最多 800px，用 `max-w-2xl`；
3. 页面放在 `app/`，数据脚本放在 `scripts/`，Markdown 源文件放在 `content/`；
4. 预构建脚本用 Node.js (mjs) 或 Python，零依赖检查用 bash + perl。
