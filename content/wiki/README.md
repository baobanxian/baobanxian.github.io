# Wiki 记忆规则

1. **写入前先搜索**是否已有同义页面。
2. **优先更新已有页面**，不重复创建。
3. **区分「我的原话」「Agent 总结」「外部来源」**。
4. **不把未经确认的 Agent 推断写成我的观点**。
5. **每页包含 `updated` 和 `tags`**。
6. **新页面至少连接一个已有页面**。
7. **修改 Wiki 也必须经过 git diff**。
