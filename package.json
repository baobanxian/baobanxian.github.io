{
  "name": "my-portfolio",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "predev": "npm run generate && npm run generate-wiki && npm run fetch-articles",
    "dev": "next dev",
    "generate": "node scripts/generate-posts.mjs",
    "generate-feed": "node scripts/generate-feed.mjs",
    "fetch-articles": "node scripts/fetch-articles.mjs",
    "check-feeds": "node scripts/check-feeds.mjs",
    "validate-feeds": "node scripts/validate-feeds.mjs",
    "generate-wiki": "node scripts/generate-wiki.mjs",
    "prebuild": "npm run generate && npm run generate-wiki && npm run generate-feed; npm run fetch-articles; npm run check-feeds; npm run validate-feeds",
    "build": "next build",
    "start": "next start",
    "lint": "eslint"
  },
  "dependencies": {
    "next": "16.2.10",
    "react": "19.2.4",
    "react-dom": "19.2.4"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4",
    "@types/node": "^20",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "eslint": "^9",
    "eslint-config-next": "16.2.10",
    "tailwindcss": "^4",
    "typescript": "^5"
  }
}
